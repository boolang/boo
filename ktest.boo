f main() {
    // malloc(32);
    // nop();
    // i (n) {
    //     print("No");
    // } e i (n) {
    //     print("Yes");
    // } e {
    //     print("Else");
    // }
    // do_exit<S>(43);
    // do_exit<I>(43);
    check(n);
    exit(0);
}

f check(var: B) {
    var = y;
    i (n) {
        print("Fizz");
    } e {
        print("Buzz");
    }
}

f nop() {}
